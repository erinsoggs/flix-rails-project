<strong><%= @movie.title %> <br></strong>
<br>
<%= @movie.description %>

<section class="admin">
  <%= link_to "Edit", edit_movie_path(@movie), class: "button" %>
  <%= link_to "Delete", movie_path(@movie), class: "button", method: :delete, data: { confirm: "Are you sure you want to delete this?" } %>
</section>